<!DOCTYPE html>
<html data-theme="cupcake">
  <head>
    <title>JournalApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_pack_tag "application" %>
  </head>

  <body>
    <main class="container mx-auto h-screen pt-28 prose max-h-screen">
      <% if alert %>
        <div class="alert alert-error">
          <span><%= alert %></span>
        </div>
      <% end %>
      <% if notice %>
        <div class="alert alert-info">
          <span><%= notice %></span>
        </div>
      <% end %>
      <% if user_signed_in? %>
        <p><%= current_user.email %></p>
        <%= button_to "log_out", destroy_user_session_path, method: :delete %>
        <%= link_to "Home", root_path, class: "btn btn-sm btn-ghost mb-5 max-w-sm gap-2 justify-start" if params[:action] != "home"  %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
