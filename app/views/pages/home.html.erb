<div>
  <h1>Tasks for today</h1>
  <div class="flex flex-col gap-2">
    <% Task.group_per_deadline["today"].each do |task| %>
      <%= render task, category: @categories.find {|category| category.id == task.category_id} %> 
    <% end %>
  </div>
</div>
